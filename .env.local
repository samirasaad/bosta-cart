# Copy this file to .env.local and fill in values.
# Required for production; optional for local (defaults apply).
#
# Public base URL of the app (metadata, share links, canonical URLs).
# In production set to your deployed URL (e.g. https://bosta-cart.vercel.app).
NEXT_PUBLIC_APP_URL=http://localhost:3000
#
# API base URL. Default: Fake Store API.
NEXT_PUBLIC_API_BASE_URL=https://fakestoreapi.com
#
# Site name in metadata (Open Graph, page titles). Optional.
NEXT_PUBLIC_SITE_NAME=Bosta Cart
